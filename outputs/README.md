# outputs

`outputs/` 用于存放本地运行产物。

仓库建议只保留：

- 目录结构
- `.gitkeep`
- 本说明文件

默认不要提交：

- 生成的 Markdown / JSON 报告
- 下载的 PDF
- `prompt_requests/` 下的 prompt 回放文件
- 任何带有本机绝对路径、个人研究主题或临时实验内容的运行结果

如果你需要公开分享样例，请先手动脱敏，再放到单独的示例目录，而不是直接提交原始运行产物。
